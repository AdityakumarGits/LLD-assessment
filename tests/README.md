# Tests

The included validator tests cover:

- empty submission rejection
- valid structured submission acceptance

To run them from the repository root after installing Jest:

```bash
cd tests
npm install
npm test
```

The prototype can be extended with API tests using Supertest.
